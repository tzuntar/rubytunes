<div class="body-flex">
  <%= render 'layouts/sidebar' %>
  <div class="root-container">
    <%= render 'layouts/navbar' %>

    <main>
      <div class="margin-top-20">
        <%= render @album %>
        <%= render partial: 'songs', locals: {album: @album} %>
      </div>
    </main>

  </div>
</div>
