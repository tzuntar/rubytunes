<div class="song-list-card">
  <div class="flex-container">
    <div>
      <%= album_art_or_placeholder card, 'album-art-list' %>
    </div>
    <div class="flex-container flex-grow-right">
      <div>
        <p><%= link_to card.title, url_for(card) %></p>
        <p class="sub-label">
          <% card.artists.each do |artist| %>
            <%= link_to artist.name, url_for(artist) %>
          <% end %>
        </p>
      </div>
      <!--<?php if ($adminEditControls) { ?>
      <div>
        <p><a href="edit_song.php?id=<?= $song->id ?>" class="action-link">Edit →</a></p>
        <p><a href="delete_song.php?id=<?= $song->id ?>" class="action-link">Delete</a></p>
      </div>
      <?php } ?>-->
    </div>
  </div>
</div>